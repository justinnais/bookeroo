export enum AccountType {
    STANDARD,
    BUSINESS,
    ADMIN,
}

export enum AccountStatus {
    ACTIVE = "ACTIVE",
    PENDING = "PENDING",
    REJECTED = "REJECTED",
    BANNED = "BANNED",
    INACTIVE = "INACTIVE",
}

export enum BookCondition {
    NEW = "New",
    GOOD = "Good",
    FAIR = "Fair",
    POOR = "Poor",
}
