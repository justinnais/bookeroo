export enum AccountType {
    STANDARD,
    BUSINESS,
    ADMIN,
}

export enum AccountStatus {
    OK,
    PENDING,
    REJECTED,
    BANNED,
    INACTIVE,
}

export enum BookCondition {
    NEW = "New",
    GOOD = "Good",
    FAIR = "Fair",
    POOR = "Poor",
}
