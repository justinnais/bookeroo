export enum AccountType {
    STANDARD,
    BUSINESS,
    ADMIN,
}

export enum AccountStatus {
    OK = "OK",
    PENDING = "PENDING",
    REJECTED = "REJECTED",
    BANNED = "BANNED",
    INACTIVE = "INACTIVE",
}
